{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Pytest",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/tests",  // lub ścieżka do konkretnego testu
            "console": "integratedTerminal",
            "justMyCode": false, // Debuguj także kody bibliotek
            "args": ["-s"]
        }
    ]
}
